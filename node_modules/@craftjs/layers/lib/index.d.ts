import React from 'react';
import { LayerOptions } from './interfaces';
export { useLayer, DefaultLayer, DefaultLayerHeader, EditableLayerName, } from './layers';
export declare const Layers: React.FC<Partial<LayerOptions>>;
